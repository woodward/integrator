defmodule Integrator.RungeKuttaTest do
  @moduledoc false
  use Integrator.TestCase, async: true

  describe "compute_step/8" do
    # Note that compute_step/8 is tested extensively in `RungeKutta.StepTest.exs`, so those tests are
    # not repeated here.
  end
end
